<?php

namespace Tree6bee\Any\Helpers;

/**
 * 框架日期辅助类
 */
class Date
{
    /**
     * 根据生日生成星座
     * @todo 可以参考jy.inc/function.inc.php(generate_zodiac)
     */

    /**
     * 阴历转阳历
     */

    /**
     * 阳历转阴历
     */

    /**
     * 根据生日计算生肖
     */

    /**
     * 根据生日计算年龄
     */
    public static function getAge($birthyear, $birthday, $year = false, $date = false)
    {
        // if ($year === false) {
        //     $year = date('Y');
        // }
        // if ($date === false) {
        //     $date = date('md');
        // }
        // $add_age = $birthday < $date ? 0 : -1;
        // $age = $year - $birthyear + $add_age;
        // if ($age < 18) { //@todo 是否需要去掉
        //     $age = 18;
        // }
        // return $age;
    }
}
